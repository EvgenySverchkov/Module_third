import React from 'react';
import { shallow } from 'enzyme';

import UserInfoTable from './UserInfoTable.js';

describe("UserInfoTable request tets", ()=>{
	const mockFn = jest.fn();
	const nextProps = {
		idUser: 1,
		backHandler: ()=>{}
	}
	it("snapshot test for Profile", ()=>{
		
	});
});